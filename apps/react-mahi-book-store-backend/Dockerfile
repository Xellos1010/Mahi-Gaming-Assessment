# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build react-mahi-book-store-backend`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t react-mahi-book-store-backend`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system react-mahi-book-store-backend && \
          adduser --system -G react-mahi-book-store-backend react-mahi-book-store-backend

COPY dist/apps/react-mahi-book-store-backend react-mahi-book-store-backend/
RUN chown -R react-mahi-book-store-backend:react-mahi-book-store-backend .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix react-mahi-book-store-backend --omit=dev -f install

CMD [ "node", "react-mahi-book-store-backend" ]
